server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/analyticsservice
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

